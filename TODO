* Somehow check memory allocations in dump() function
** this might require a more major reworking of that bit of code
* Replace assert in dump() function
** there is one in resolve_any_address() that can be left as-is
* Fix mmdb* apps
** better usage info and error messages
** more shared code
** don't assume GeoIP2 data format in mmdblookup
* More tests
** tests for apps
* Some sort of data validation testing when decoding a data entry
